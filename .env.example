# Django backend environment variables.
# Copy to .env and fill in real values — never commit secrets.

DJANGO_SECRET_KEY=your-secret-key-here
DB_NAME=prosperitydb
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
