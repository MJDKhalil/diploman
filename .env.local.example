# Copy this file to .env.local and fill in values for local development.
# Leave NEXT_PUBLIC_API_URL empty to run in demo mode (no backend required).

# Backend API base URL (e.g. http://localhost:8000)
# Leave empty to enable demo mode — all form submissions will be disabled.
NEXT_PUBLIC_API_URL=
